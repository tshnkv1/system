import React from 'react';
import { Row, Col, Spin } from 'antd';

const DeviceOnMap = () => {
     
      return (
        <div>
          DeviceOnMap
        </div>
      );
}

export default DeviceOnMap;